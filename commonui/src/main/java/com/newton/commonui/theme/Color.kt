package com.newton.commonui.theme

import androidx.compose.ui.graphics.Color

val DemocraticGreen = Color(0xFF006B3C)
val DemocraticGreenDark = Color(0xFF004D2A)
val DemocraticGreenLight = Color(0xFF4CAF50)

val PatrioticRed = Color(0xFFCE1126)           // Kenya flag red
val PatrioticRedDark = Color(0xFF8B0000)       // Dark red for emphasis
val PatrioticRedLight = Color(0xFFEF5350)      // Light red for backgrounds

val UnityBlack = Color(0xFF000000)             // Kenya flag black - strength
val PeaceWhite = Color(0xFFFFFFFF)             // Kenya flag white - peace
val UnityGrey = Color(0xFF424242)              // Softer black alternative

val JusticeBlue = Color(0xFF1976D2)            // Trust, justice, stability
val JusticeBlueDark = Color(0xFF0D47A1)        // Darker blue for contrast
val JusticeBlueLight = Color(0xFF42A5F5)       // Light blue for highlights

val TransparencyGold = Color(0xFFFFA000)       // Transparency, enlightenment
val TransparencyGoldDark = Color(0xFFFF8F00)   // Darker gold
val TransparencyGoldLight = Color(0xFFFFCC02)  // Bright gold for accents

val EqualityPurple = Color(0xFF7B1FA2)         // Equality, dignity
val EqualityPurpleLight = Color(0xFFBA68C8)    // Light purple for backgrounds

// Surface and Background Colors
val SurfacePure = Color(0xFFFFFFFF)            // Pure white surfaces
val SurfaceLight = Color(0xFFFAFAFA)           // Light grey surface
val SurfaceGreenTint = Color(0xFFF1F8E9)       // Very light green tint
val BackgroundCream = Color(0xFFFFFDF7)        // Warm, welcoming background

val TextPrimary = Color(0xFF1A1A1A)            // Almost black, softer than pure black
val TextSecondary = Color(0xFF616161)          // Medium grey for secondary text
val TextTertiary = Color(0xFF9E9E9E)           // Light grey for hints/placeholders
val TextOnDark = Color(0xFFFFFFFF)             // White text on dark backgrounds

// Semantic Colors - Status and feedback
val SuccessGreen = Color(0xFF2E7D32)           // Success states
val SuccessGreenLight = Color(0xFFC8E6C9)      // Success backgrounds
val ErrorRed = Color(0xFFD32F2F)               // Error states
val ErrorRedLight = Color(0xFFFFCDD2)          // Error backgrounds
val WarningAmber = Color(0xFFFF8F00)           // Warning states
val WarningAmberLight = Color(0xFFFFE0B2)      // Warning backgrounds
val InfoBlue = Color(0xFF1976D2)               // Information states
val InfoBlueLight = Color(0xFFBBDEFB)          // Information backgrounds

val LinkBlue = Color(0xFF1565C0)               // Links and interactive elements
val HoverGrey = Color(0xFFF5F5F5)              // Hover states
val PressedGrey = Color(0xFFE0E0E0)            // Pressed states
val DisabledGrey = Color(0xFFBDBDBD)           // Disabled elements
val DividerGrey = Color(0xFFE0E0E0)            // Dividers and borders

val VotingBlue = Color(0xFF3F51B5)             // Voting, civic engagement
val CitizenshipGold = Color(0xFFFF6F00)        // Citizenship, participation
val RightsIndigo = Color(0xFF303F9F)           // Rights, law, constitution
val GovernanceNavy = Color(0xFF1A237E)